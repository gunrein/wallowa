{
  "name": "opsql",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:css": "npx tailwindcss -i styles.css -o ./static/styles.css --watch",
    "dev:backend": "cargo run",
    "dev": "npm-run-all --parallel dev:*"
  },
  "devDependencies": {
    "daisyui": "^3.1.6",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.3.2"
  }
}
